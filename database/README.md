# Onboarding slice

Landing, register/login (development JWT), tenant, business, location, plan selection, dashboard.

- Schema: `dp`
- Plans seeded in `DatabaseSeeder`
- EF migrations live in `src/DigitalPulse.Infrastructure/Persistence/Migrations`
